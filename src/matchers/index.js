const DomainMatcher = require('./DomainMatcher')
const PlainMatcher = require('./PlainMatcher')
const RegexMatcher = require('./RegexMatcher')

module.exports = {
    DomainMatcher,
    PlainMatcher,
    RegexMatcher,
}
